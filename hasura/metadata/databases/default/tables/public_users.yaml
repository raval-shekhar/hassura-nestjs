table:
  schema: public
  name: users
object_relationships:
  - name: user_tracking
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: user_trackings
